# Runtime dependencies.
#
# pyproject.toml is the source of truth (`pip install -e .`). This file exists
# for tooling that expects it and is kept in step with pyproject's [project]
# dependencies. Dev tools live in the `dev` extra: pip install -e ".[dev]"
#
# google-auth, cryptography, pyasn1 and pyasn1-modules were removed in T-004:
# leftovers from the Gemini era that nothing imported and that carried every
# CVE pip-audit reported.

flask>=3.1
groq>=1.6
python-dotenv>=1.2
pydantic-settings>=2.0
sentry-sdk[flask]>=2.0
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
